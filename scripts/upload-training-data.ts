/**
 * @deprecated This script needs to be migrated from Supabase to Firebase.
 *
 * Previously uploaded training data from CSV to Supabase with embeddings.
 * TODO: Rewrite to use Firestore batch writes.
 */

console.error(
  "⚠️ upload-training-data.ts: This script is deprecated and needs Firebase migration."
);
console.error("Exiting without action.");
process.exit(1);
