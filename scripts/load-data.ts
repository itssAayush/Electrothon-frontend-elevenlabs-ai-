/**
 * @deprecated This script needs to be migrated from Supabase to Firebase.
 *
 * Previously loaded medical Q&A data with embeddings into Supabase.
 * TODO: Rewrite to use Firestore.
 */

console.error(
  "⚠️ load-data.ts: This script is deprecated and needs Firebase migration."
);
console.error("Exiting without action.");
process.exit(1);
