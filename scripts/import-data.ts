/**
 * @deprecated This script needs to be migrated from Supabase to Firebase.
 *
 * Previously imported CSV data into Supabase conversations table.
 * TODO: Rewrite to use Firestore batch writes.
 */

console.error(
  "⚠️ import-data.ts: This script is deprecated and needs Firebase migration."
);
console.error("Exiting without action.");
process.exit(1);
